# npm artifacts from per-skill dependency installation
*/node_modules/
*/package.json
*/package-lock.json

# skill credentials & runtime token caches — never commit
*/MEMORY.md
*/.token-cache
*/.token

# Per-account credential stores hold real secrets once filled in. The committed
# .<service>-credentials.json files in this repo are PLACEHOLDER example shapes only;
# never commit one containing real secrets.
